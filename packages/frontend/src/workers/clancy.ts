import "./comlink-setup"

import type { Api } from "privatefolio-backend/build/src/api/api"
// import { api } from "src/api/api"
// import { isWebWorker } from "src/utils/environment-utils"

export type Clancy = Api

// if (isWebWorker) {
// expose(api)
// console.log("WebWorker (Clancy) exposed")
// }
